---
phase: 01-tutorial-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md, .planning/codebase/CONVENTIONS.md]
autonomous: true
requirements: [TUT-01, TUT-04]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Repository instructions protect README as a tutorial index and not a course-design document."
    - "D-03/D-04: Repository instructions treat primer as concept reference material, not mandatory pre-reading."
    - "D-05/D-06: Repository instructions and conventions treat basic_operation as lookup/reference material."
    - "D-07/D-08/D-09: Repository instructions tell future agents to treat source_materials as source references to distill into Markdown."
    - "Repository conventions no longer require submission/scoring sections."
  artifacts:
    - "AGENTS.md contains tutorial path guardrails."
    - ".planning/codebase/CONVENTIONS.md contains practice/self-check language."
  key_links:
    - "AGENTS.md references source_materials as source references."
    - ".planning/codebase/CONVENTIONS.md aligns with PROJECT.md tutorial-only scope."
---

<objective>
Align repository maintenance instructions and codebase-map conventions with tutorial-only scope.

Purpose: Prevent future GSD phases or agents from reintroducing evaluation, rubric, teacher-delivery, or submission-centric language.
Output: Updated `AGENTS.md` and `.planning/codebase/CONVENTIONS.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tutorial-foundation/01-CONTEXT.md
@.planning/phases/01-tutorial-foundation/01-RESEARCH.md
@.planning/phases/01-tutorial-foundation/01-PATTERNS.md
@AGENTS.md
@.planning/codebase/CONVENTIONS.md
</context>

<threat_model>
No security-sensitive behavior is introduced. This plan edits documentation and planning metadata only. Main risk is stale instructions that steer later work back toward evaluation mechanisms; verification uses grep checks.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen tutorial-only maintenance instructions</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/phases/01-tutorial-foundation/01-CONTEXT.md</read_first>
  <action>Implement D-01 through D-09 as maintenance guardrails in `AGENTS.md`. Update `AGENTS.md` so the Working Principles include these concrete rules: preserve the tutorial path `README.md -> labs/` as the main operational route; treat `primer/` as concept reference material; treat `basic_operation/` as lookup/reference material; treat `source_materials/` as source documents to be distilled into Markdown when useful; do not add non-tutorial workflow layers unless scope changes explicitly. Keep existing rules about copyable commands, relative links, detector hardware verification, and ignored generated outputs.</action>
  <verify>Run `rg -n 'README.md -> labs/|primer/|basic_operation/|source_materials/|non-tutorial workflow layers' AGENTS.md`.</verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `README.md -> labs/`
    - `AGENTS.md` contains `primer/`
    - `AGENTS.md` contains `basic_operation/`
    - `AGENTS.md` contains `source_materials/`
    - `AGENTS.md` contains `non-tutorial workflow layers`
  </acceptance_criteria>
  <done>Repository instructions protect the tutorial-only architecture and source-material treatment.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale documentation conventions</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <read_first>.planning/codebase/CONVENTIONS.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement D-01 through D-06 as codebase-map conventions. Update `.planning/codebase/CONVENTIONS.md` so the Documentation section says each lab should contain: goal, prerequisites, steps, placeholders to modify, expected outcomes, practice records, and self-checks. Remove stale requirements that say labs must include submission requirements or scoring points. Add a bullet that README is an index-style entry and that `basic_operation/` is lookup reference material. Preserve the Commands and Python sections unless a wording change is needed for consistency.</action>
  <verify>Run `rg -n 'practice records|self-checks|index-style|lookup reference' .planning/codebase/CONVENTIONS.md`; run `rg -n 'submission requirements|scoring points|评分|评价|rubric|提交要求' .planning/codebase/CONVENTIONS.md` and confirm it exits 1.</verify>
  <acceptance_criteria>
    - `.planning/codebase/CONVENTIONS.md` contains `practice records`
    - `.planning/codebase/CONVENTIONS.md` contains `self-checks`
    - `.planning/codebase/CONVENTIONS.md` contains `index-style`
    - `.planning/codebase/CONVENTIONS.md` contains `lookup reference`
    - `rg -n 'submission requirements|scoring points|评分|评价|rubric|提交要求' .planning/codebase/CONVENTIONS.md` exits 1
  </acceptance_criteria>
  <done>Codebase convention map no longer nudges future work toward submission/scoring mechanics.</done>
</task>

</tasks>

<verification>
- [ ] `rg -n 'README.md -> labs/|primer/|basic_operation/|source_materials/' AGENTS.md`
- [ ] `rg -n 'practice records|self-checks|index-style|lookup reference' .planning/codebase/CONVENTIONS.md`
- [ ] `rg -n 'submission requirements|scoring points|评分|评价|rubric|提交要求' .planning/codebase/CONVENTIONS.md` exits 1
- [ ] `python3 -m py_compile tools/detect_system.py`
</verification>

<success_criteria>

- `AGENTS.md` protects tutorial-only architecture.
- `.planning/codebase/CONVENTIONS.md` no longer contains stale submission/scoring requirements.
- Future GSD agents have clear rules for `primer/`, `basic_operation/`, and `source_materials/`.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-tutorial-foundation/01-02-SUMMARY.md`
</output>
